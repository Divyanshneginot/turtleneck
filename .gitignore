# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Patch bundles (developer scratch output — never commit these)
*.patch
*.diff

# Turtleneck install output (created by scripts/install.py inside a target project)
.turtleneck/
.agents/
*.turtleneck.bak
*.turtleneck.bak.*

